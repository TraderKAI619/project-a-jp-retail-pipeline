# Core data processing
pandas>=2.0.0
openpyxl>=3.1.0          # For reading Excel files (.xlsx)

# Optional: DuckDB for demos
duckdb>=0.9.0

# Optional: If you need to write Excel files
xlsxwriter>=3.1.0

# Data validation (if you use)
# pandera>=0.17.0
